# Public URL of the PocketBase server (safe to expose to the browser — it is
# not a secret, it is just the API endpoint the client talks to).
# Do NOT put admin/superuser credentials or secret keys here or anywhere
# prefixed with PUBLIC_.
PUBLIC_POCKETBASE_URL=https://api.zozzo.fr
