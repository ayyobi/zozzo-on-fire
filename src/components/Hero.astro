<section id="accueil" class="landing-hero">
 <img src="/images/bg-hero.avif" alt="Zozzo allongée dans l’herbe, entourée de livres et de fleurs" width="2880" height="2048" fetchpriority="high" />
 <div class="hero-copy">
  <h1>Apprendre la langue des<br class="desktop-break" /> signes enfant</h1>
  <p class="tagline">Des histoires qui valent plus que des mots.</p>
  <p class="description">Votre enfant passe du temps sur les écrans.<br />Et si ce temps devenait utile ?<br />Avec Zozzo, il apprend la langue des signes facilement, développe son empathie et découvre une nouvelle façon de communiquer.</p>
  <a href="#telecharger">Télécharger Zozzo</a>
 </div>
</section>
<style>
.landing-hero { position: relative; width: 100%; max-width: none; min-height: calc(100svh - 82px); background: #d7df7e; overflow: hidden; isolation: isolate; }
img { position: absolute; inset: 0; width: 100%; height: 100%; object-fit: cover; object-position: center 20%; }
.hero-copy { position: relative; padding-top: clamp(75px, 8.9vw, 128px); padding-bottom: 48px; margin-left: 34.65%; width: 55%; max-width: 750px; color: #080900; }
h1 { font-size: clamp(36px, 4.3vw, 44px); font-weight: 400; line-height: 1.12; letter-spacing: -.5px; }
.tagline { margin-top: 28px; font-size: clamp(20px, 2.2vw, 22px); font-weight: 500; line-height: 1.35; }
.description { margin-top: 20px; font-size: 17px; font-weight: 400; line-height: 1.5; max-width: 700px; }
a { display: inline-flex; margin-top: 28px; padding: 16px 24px; border-radius: 50px; background: #f26748; color: #f3e63e; font-size: 20px; line-height: 1.3; font-weight: 700; }
a:hover { background: #e95537; }
@media (min-width: 768px) and (max-width: 1023px) {
img { object-position: left 20%; }
}
@media (min-width: 1024px) {
.landing-hero { height: auto; min-height: 0; aspect-ratio: 2880 / 1870; }
img { object-fit: cover; object-position: center bottom; }
.hero-copy { padding-top: 8.9vw; padding-bottom: 3.3vw; margin-left: 34.65%; width: 51%; max-width: 800px; }
h1 { font-size: var(--desktop-title); }
.tagline { margin-top: 24px; font-size: 20px; }
.description { margin-top: 16px; font-size: var(--desktop-body); }
a { margin-top: 24px; padding: 12px 22px; font-size: var(--desktop-button); }
}
@media (max-width: 767px) {
.landing-hero { height: auto; aspect-ratio: auto; min-height: calc(100svh - 72px); }
img { position: absolute; inset: 0; width: 100%; height: 100%; object-fit: cover; object-position: left bottom; }
.hero-copy { margin: 0; width: 100%; max-width: none; padding: 160px 24px 64px; }
h1 { font-size: clamp(28px, 7.6vw, 34px); letter-spacing: -.5px; }
.desktop-break { display: none; }
.tagline { margin-top: 20px; font-size: 18px; }
.description { margin-top: 16px; font-size: 16px; line-height: 1.5; }
a { margin-top: 24px; padding: 16px 22px; font-size: 18px; }
}
</style>
